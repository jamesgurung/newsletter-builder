@page
@{
  var returnUrl = Request.Query["path"].FirstOrDefault();
}
<p style="text-align: center; margin-top: 18px">
  <a href="/auth/login/challenge@(string.IsNullOrEmpty(returnUrl) ? string.Empty : $"?path={returnUrl}")">Sign in with Microsoft</a>
</p>